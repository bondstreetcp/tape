# Copy to the repo root as .env.local on the clean-IP worker (chmod 600). Values live in the NAS tape.env.
LAKE_S3_ENDPOINT=
LAKE_S3_BUCKET=
LAKE_S3_KEY_ID=
LAKE_S3_SECRET=
OPENROUTER_API_KEY=
# optional
# TAPE_WRITER=worker
# CALL_DIGEST_BUDGET_MIN=30
