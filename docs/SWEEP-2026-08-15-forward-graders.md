# Forward-grader bug sweep — 2026-08-15 (39-agent find→verify)

33 of 34 findings survived adversarial verification. The two classes: **cross-feed clock divergence** (grading frozen entries against a possibly-stale sibling feed) and **failure-vs-absence** (one sentinel for both a 404 and a dead network). FIXED same day (d9a4714d, 62ca0770): the three history-wipe reads (signal-log/trade-log/preview-log ENOENT-only), signal-log stale-snapshot vintage gate + spx-null entry deferral. **STATUS 2026-08-15 late: 23 of 25 worked down same-day** (27ac76fd, 7f91df04, 3251d80e, fdce9de9, 576c6773, 03b12941, a434bd6e, 1721601b — plus the horizon-aware track record 73a17e22). **The two DELIBERATE deferrals:**
- **[HIGH] VRP per-row cone staleness (refresh-vol-premium-ledger)** — needs vol-cone to stamp each row's own last-series date (a feed schema change + writer edit). No deadline pressure: the ledger's first grades mature ~Sep; do it before then.
- **[LOW] valuation 'current' multiple pinned to first close after quarter-end** — a methodology change to PUBLISHED numbers; this file has a documented output-preservation trap (the KEEP=200 incident), so it needs a byte-regression exercise like that one, not a drive-by edit.

The original ranked list (kept for the failure scenarios + fix hints):

## [HIGH·cross-feed-clock] refresh-trade-log.ts:278 — Post-print grade fires on the snapshot-frozen scheduled earnings date with zero verification the print happened — a rescheduled print grades a random non-event session, permanently

**Failure:** SYM ($2bn+, so it passes MIN_MKTCAP) is logged as a rich-verdict short strangle for its scheduled 2026-08-20 AMC print (rec.earningsDate frozen from the snapshot's Yahoo earningsTimestampStart). The company moves the print to 08-27 (delayed 10-Q). The 08-22 nightly run (~01:30 UTC, past the 21:30Z session-complete gate at :87): status!=="settled" and now>=eT (:278) → reactionFromChart returns 08-21's ordinary session, e.g. -0.4% (first bar with day > "2026-08-20"). The rec is written with realizedMovePct=-0.40, moveCleared=false, spotAtEarnings=the 08-21 close, and settlePostPrint strips the f

**Fix:** Before grading at :278, verify the print actually happened — never settle on the frozen clock alone. Cheapest in-repo ground truth: detectRecentReport(rec.symbol, eT-anchored) from lib/preannounce.ts (8-K Item 2.02 filed within ~±5d of eDay; the log is US-only so submissions coverage is complete). Contract must avoid reintroducing Class B: (a) confirmed report → grade as today; (b) confirmed NO report near eDay + a fresher date available (re-read the current snapshot's earningsDate for the symbo

## [HIGH·failure-vs-absence] refresh-trade-log.ts:263 — Preannounce re-stamp persists a transport failure as 'looked, clean' (null) — permanent false negatives; and a log-time SEC failure logs plays the desk's own rules withhold

**Failure:** One throttled or slow data.sec.gov night (the measured 2026-07-15 class, or the 08-11..15 outage window): each getSubmissions call 429s/retries past the 6s bounded race, so every detectPreannounce resolves null. Pass 1b (refresh-trade-log.ts:251-266) walks every rec in the book with catalystFlag===undefined and line 263 stamps each one catalystFlag:null — "looked, clean". JSON persists null; from the next run the ===undefined gate at line 260 skips them all forever. Any of those names that provably filed a prelim 8-K Item 2.02 2-35d before its print (the IBM case) is now a permanent false nega

**Fix:** Make the tri-state real at the source: detectPreannounce should return undefined (or {ok:false}) when the look did not complete — the bounded race resolving null for tickerToCik/getSubmissions, and the catch at lib/preannounce.ts:117 — and null only when a full submissions walk completed clean; never memoize failures (memo.set only on ok). Then refresh-trade-log.ts:263 only persists null when pre !== undefined completed-clean, leaving the field absent on failure so tomorrow's run retries (the ==

## [HIGH·failure-vs-absence] refresh-insiders.ts:146 — Insider scan conflates EDGAR transport failure with 'no buys' at every layer, then writes insiders.json raw with no guard and no floor

**Failure:** Nightly FULL tick, EDGAR intermittently 429s during the ~2,600-name Form 4 scan (documented in lib/edgar.ts:48-50 as a real, recurring behavior for bulk scans). For any symbol whose in-window Form 4 XML fetch hits even ONE transient 429/timeout, parseForm4 (lib/edgar.ts:187-190) returns [] with no retry → scanSymbol (refresh-insiders.ts:71) returns null → the name is written out of insiders.json as "no buys". Under a sustained 20-minute throttle, hundreds of names drop; symbols whose getForm4List retries exhaust are silently skipped by the catch at refresh-insiders.ts:120-122. The shrunken map

**Fix:** Distinguish failure from absence at each layer, then floor the write — never trading correctness for availability: (1) lib/edgar.ts parseForm4: switch to fetchWithRetry and return `InsiderTx[] | undefined` — undefined on transport failure (thrown/429/5xx after retries), [] only for a parsed filing with no non-derivative transactions (404 may stay [] — genuinely not there). This mirrors the already-established lib/secFrames undefined-on-failure + detectorOk pattern. Audit parseForm4's other calle

## [HIGH·cross-feed-clock] refresh-vol-premium-ledger.ts:58 — VRP ledger's cone freshness check is file-level only; a per-row-stale cur20 under a fresh stamp grades a dead window

**Failure:** Reproducing conditions, all verified in code: an open pick (entry 08-01, atmIVEntry 0.90) whose symbol's series file stops gaining bars mid-hold (halt/delist/ticker-change on ~08-10, or the refresh-series step failing multiple nights while the tick continues — continue-on-error, run-tick.ts:20/479) but keeps its ≥60 historical bars and stays in any universe snapshot. refresh-vol-cone rebuilds nightly from the frozen file (no network, no last-bar check) and stamps fresh generatedAt; cur20≈0.32 is computed over the trailing 21 pre-freeze returns (lib/volCone.ts:123), with any >±65% deal-pop day 

**Fix:** In refresh-vol-cone.ts, capture the series' last bar date (last day from bucketByDay before returns are taken — e.g. thread it out of logReturnsOf or read daily's last element) and add `lastBar: string` to VolConeFeedRow (lib/volCone.ts:52-66) — one small field, file already rounds numbers to stay compact. In the ledger, read {symbol, cur20, lastBar}, and extend rvUsable/maturityDecision so "grade" additionally requires the row's lastBar to be within CONE_TOLERANCE_DAYS of `today` (equivalently 

## [HIGH·failure-vs-absence] news.ts:113 — getNews returns [] for a total transport failure — identical to "no news exists"

**Failure:** Nightly FULL run; Google News egress from the NAS fails for the run (network block like the 08-15 Yahoo block) or Google 429s a subset of the ~dozens of concurrent RSS requests. (1) refresh-catalysts.ts: symbol XYZ popped 15% today on a takeover; its prior catalysts.json entry is TTL-expired so it regenerates. getNews returns [] (both queries dead, or only the 120d press-release query survives and pickHeadlines date-gates it to nothing for the 5d 1d-window). Line 124 writes { why: "", ts: <tonight> } — the prior good why is gone, and line 105 reuses the fresh-stamped empty entry for the next 1

**Fix:** Distinguish at the source, then route through guards that already exist. In getNews, count successful query fetches; if zero succeeded, THROW (undefined-on-failure equivalent) instead of returning [] — but note the fix is two-part, because call sites like refresh-catalysts.ts:121 wrap the call in `.catch(() => [])`, which would convert the throw straight back into the sentinel. Audit the 13 call sites: browsing/UI routes (api/news, industry-feed, watchlist-*) may legitimately keep the []-fallbac

## [HIGH·failure-vs-absence] refresh-catalysts.ts:121 — News outage persists a fresh-stamped why:"" — the anti-clobber guard is bypassed because getNews returns [] instead of throwing

**Failure:** news.google.com is unreachable from the NAS (DNS failure, connection reset, or 12s deadline abort — both queries in getNews's loop throw and are swallowed) during one nightly refresh-catalysts run. Every symbol in `todo` — all TTL-expired movers, including ones whose prior entry held a good, still-true catalyst like an agreed acquisition, plus every new mover — gets `{ why: "", ts: <tonight>, tf }` written into data/catalysts.json via line 124, bypassing the lines 126-131 preservation logic. The file ships with a fresh generatedAt so no freshness alert fires. After connectivity returns, line 1

**Fix:** Make getNews distinguish transport failure from legitimate emptiness — e.g. track fetchOk per query and return undefined (or throw, or return {items, ok}) when any/all queries transport-failed, mirroring the secFrames undefined-on-failure + detectorOk pattern (safest: any failed query = failure, since the two queries cover disjoint windows and a missing when:30d query can hide today's story). In refresh-catalysts.ts, route the news-failure case through the same path as the lines 126-131 catch: k

## [HIGH·failure-vs-absence] refresh-desk-note.ts:145 — Desk note asserts "no name-specific news in the causal window" to the LLM when the news fetch died — publishing a false attribution

**Failure:** Morning run of refresh-desk-note on a day news.google.com is unreachable from the runner (timeout/DNS/firewall — the documented NAS egress-block class) or returns 429 on both RSS queries (silently dropped at lib/news.ts:115, no retry). A mover gaps +14% on a takeover reported by Reuters that morning: no 8-K Item 2.02 → detectRecentReport null (a deal 8-K is Item 1.01/8.01, lib/preannounce.ts:44-45); the catalyst cache holds why:"" with a fresh ts because refresh-catalysts.ts:121 hit the same dead upstream that night → freshDayCatalyst returns "". getNews returns [] → refresh-desk-note.ts:145 h

**Fix:** Make failure distinguishable at the source, secFrames-style: getNews tracks per-query outcomes and returns undefined (or { items, ok }) when ALL queries failed (fetch error or !res.ok) — [] stays reserved for a successful fetch with zero qualifying headlines. In refresh-desk-note: on undefined, emit a distinct driver line ("news fetch FAILED this run — headline absence is NOT evidence") plus a run-level instruction barring the Unexplained tag and absence-based positioning reads when zero news fe

## [HIGH·failure-vs-absence] preannounce.ts:76 — detectRecentReport returns null for BOTH "did not report" and "EDGAR unreachable" — and memoizes the failure as a same-day permanent negative

**Failure:** Nightly refresh-desk-note run, right after other EDGAR-heavy refresh steps have been hammering data.sec.gov. Stock X reported that morning (8-K Item 2.02 on record, +11% mover, lands in moverRows). Its getSubmissions call draws one 429 → fetchWithRetry sleeps ~0.5-1s and refetches a multi-MB submissions JSON → total exceeds 6s → the race at preannounce.ts:59-60 resolves null → out=null, indistinguishable from "did not report", memoized at line 77 for the rest of the day. refresh-desk-note.ts:147 skips the "REPORTED EARNINGS TODAY" prefix; the SYSTEM prompt's EARNINGS-OUTRANK-EVERYTHING rule ne

**Fix:** Apply the lib/secFrames detectorOk pattern: make detectRecentReport return undefined (or {ok:false}) on timeout/transport failure vs null only on a checked absence (submissions successfully fetched, loop completed), and memoize ONLY checked results — never the failure sentinel (preannounce.ts:77). Same split for detectPreannounce's memo at line 118. In refresh-desk-note, the nightly script can afford correctness: on unknown, retry once without the 6s race (fetchWithRetry already has bounded budg

## [MEDIUM·cross-feed-clock] capture-trade-spreads.ts:57 — No now-vs-earningsDate guard in the capture queue: the LAST liquidity capture for nearly every play lands on the post-print session, overwriting the pre-print read the field claims to be

**Failure:** Play logged Monday night for a Tuesday-AMC reporter. Tuesday ~15:00 UTC intraday tick: capture measures the pre-print chain, liveSpreadPct = 0.55 → computeRiskFlags adds "wide-spread", liquidityTier = "wide" (correct: a market fill eats the credit). Tuesday ~20:30 UTC the name prints; IV crushes and reaction-day volume tightens the book. Wednesday ~01:00 UTC nightly: reactionFromChart returns null (Wednesday session hasn't traded) → status stays "awaiting_print". Wednesday ~15:00 UTC tick: due filter passes (spreadCapturedAt = Tuesday ≠ today), capture refetches the post-print chain, measures 

**Fix:** Add a print-clock guard to the due filter in scripts/capture-trade-spreads.ts:57-59: `Number.isFinite(Date.parse(r.earningsDate)) && Date.parse(r.earningsDate) > now.getTime()`. rec.earningsDate is a full ISO instant (refresh-trade-log parses it with Date.parse and reads getUTCHours() >= 20 for AMC), so instant comparison is correct here — an AMC same-day play (eT ≈ 20:30 UTC) still gets its final pre-print capture during the 14:00-20:00 window, while a BMO same-day play (eT ≈ 11-13 UTC) is corr

## [MEDIUM·failure-vs-absence] capture-trade-spreads.ts:65 — Chain transport failure conflated with 'genuinely illiquid': a failed getOptions destroys yesterday's valid liveSpreadPct (overwritten to null), burns the day's one retry, and leaves stale riskFlags contradicting the nulled field

**Failure:** Print-eve during market hours: Yahoo starts returning 429s mid-loop. For each of the ~60 due awaiting_print recs, getOptions rejects → chain=null (line 65); line 67 stamps spreadCapturedAt=today (burning the day's only attempt — later intraday ticks filter them out at line 58); line 69 overwrites each rec's previously-captured valid liveSpreadPct with null; riskFlags are NOT recomputed (line 76 is success-path only). Line 80 persists the file. The names print that evening; the nightly settle flips status off awaiting_print, so the due filter never readmits them. Permanent result in data/trade-

**Fix:** Make the caller distinguish failure from absence: `const chain = await getOptions(...).catch(() => undefined)` — on `undefined` (transport failure) do NOT stamp spreadCapturedAt and do NOT touch liveSpreadPct (leave yesterday's read in place; optionally track a separate attemptAt/failure counter so a truly dead chain can't retry forever). Only assign `liveSpreadPct = null` when a chain was actually RETURNED and is verified absent/one-sided for the play's exact expiry (chain non-null with selecte

## [MEDIUM·cross-feed-clock] capture-trade-spreads.ts:79 — Intraday spread capture re-stamps trade-log.json's generatedAt daily — the freshness monitor can never see a dead nightly logger (the heartbeat-lie mechanism inside one feed)

**Failure:** refresh-trade-log begins crashing Monday night (e.g. an import error; continue-on-error in refresh-data.yml:605 swallows it, and on the NAS it is 1 failed step of ~50 so run-tick.ts:509's majority-fail alert stays silent). Tuesday at the first mid-session quotes tick (14:00-20:00 UTC), capture-trade-spreads finds the awaiting_print recs (still stuck — the only settle pass lives in the dead script), re-stamps their spreadCapturedAt, and writes trade-log.json with generatedAt = Tuesday (line 79-80). The FULL-run freshness gate at ~22:30 UTC (refresh-data.yml:953) reads that stamp — age ~8h, OK, 

**Fix:** Stop the capture from touching the stamp that certifies the nightly writer: delete line 79 (the file keeps the generatedAt refresh-trade-log last wrote) or replace it with a separate top-level field, e.g. `data.spreadsCapturedAt = now.toISOString()`, which nothing in dataFreshness reads. The monitor then goes STALE exactly when refresh-trade-log is dead, which is the truth. This is doctrine-clean: no data is dropped or blocked (STALE-not-EMPTY preserved — the capture still writes its liveSpreadP

## [MEDIUM·failure-vs-absence] refresh-valuation-history.ts:761 — Yahoo transport failure reads as 'evaluated and suppressed': paneled names silently drop from valuation-history.json

**Failure:** Nightly FULL run on the NAS; Yahoo throttles or the documented Yahoo block flaps mid-run so 400 of ~2,600 yf.chart calls in phase 4 throw. For each: fetchPrices returns { quotes: [] } (line 261-263) → computeFromPanel returns null at line 415 → name absent from `names`; carry-forward at line 761 skips it because panel.bySymbol[sym] exists. The write of ~1,900 names clears the 500 floor (feedGuard line 87 — the 15% drop check is bypassed above the floor) and valuation-history.json ships with a fresh generatedAt missing 400 names. That same night refresh-confluence computes the board with the "v

**Fix:** Make fetchPrices distinguish failure from absence (the secFrames/fetchPanel pattern already in this file): return undefined on catch, and {quotes,splits} only from a real response. In phase 4, collect a priceFailed set for names whose fetch returned undefined (optionally retry 429s with backoff first, mirroring secFetch). Change the carry-forward condition at line 761 to also carry paneled names in priceFailed: `if (names[sym] || (panel.bySymbol[sym] && !priceFailed.has(sym)) || !inScope.has(sym

## [MEDIUM·failure-vs-absence] refresh-earnings-preview-log.ts:167 — Settle-once gate freezes partially-graded recs: epsHit/dirHit permanently null when Yahoo actuals lag the reaction

**Failure:** Mid-cap XYZ (rec logged, status=awaiting_print) reports AMC Tuesday. Wednesday's session completes the 1-day reaction (-6.2%). Wednesday night's run: getEarningsReactions matches the 8-K (gap 1d), rx.move=-0.062 → reactionHit=true/false via gradeReaction (|move|>=0.5%); Yahoo earningsHistory hasn't posted the quarter's surprisePercent yet → rx.surprise=null → dirHit=null (line 163); the baked company cache predates the print → cachedStats().surprises has no quarter within the 110d window → actualEps=null → gradeEps returns null. Line 167: gradable=true (reactionHit alone) → lines 169-177 write

**Fix:** Split "gradable at all" from "fully graded": settle at line 167-168 only when ALL components that CAN grade have graded (eps != null && dirHit != null && (reactionHit != null || flat-print)) — treating a flat print's null reactionHit as final since gradeReaction deliberately returns null there — or when now - eT >= 14*DAY (keep the existing giveup). Alternatively, keep early settle but make the settle pass also re-visit settled recs whose settledAt is within 14d of the print and which carry null

## [MEDIUM·other-integrity] refresh-earnings-preview-log.ts:148 — Hindsight check compares a calendar date to an instant — deletes legitimate same-day forecasts as 'hindsight'

**Failure:** Peak week, pool &gt; 60 for several nights. XYZ reports AMC Thursday 2026-08-13 (earningsDate = 2026-08-13T20:00:00Z from earningsTimestampStart). It first cracks the soonest-60 on Thursday's nightly run at 07:00 UTC → logged with loggedAt = 2026-08-13T07:00Z, 13 hours BEFORE the print. The 8-K is accepted 16:05 ET Thursday → EDGAR filing date "2026-08-13". Friday's settle pass skips (reaction session not closed: idx+1 >= closes.length, earningsReaction.ts:81). Saturday's pass matches rx (gap 20h &lt; 5d), then line 148: Date.parse("2026-08-13") = 2026-08-13T00:00:00Z &lt; loggedAt 07:00Z → by

**Fix:** Carry the acceptance instant onto the reaction row: in lib/earningsReaction.ts, add e.g. `acceptance: acceptByDate.get(d) ?? null` to the EarningsReaction output (it's already fetched at :27–32 and used at :87–88). In refresh-earnings-preview-log.ts:148 compare instants: hindsight iff Date.parse(acceptanceAsET) &lt; Date.parse(rec.loggedAt) — note EDGAR acceptance is zoneless ET text, so append the ET offset before parsing (a zoneless string parses as local time, which is UTC on the runner and w

## [MEDIUM·failure-vs-absence] refresh-spac-arb.ts:144 — companyfacts transport failure conflated with 'no facts' — names silently dropped from the feed

**Failure:** Nightly run during SEC rate-limiting (or the same 403/timeout regime as the 2026-07-15 buybacks incident): one of the 12 trust-frame fetches fails → `detectorOk = false` (line 91) → all ~300 SPAC CIKs skip the reuse branch and re-pull companyfacts (~120MB total at ~8 req/s), raising 429 odds. Say 12 names' pulls fail all 3 retries → `secJson` returns `undefined` → line 144 drops each as `noTrust`. Feed writes ~140 rows (≥ floor 40, guard line 87 passes), so 12 SPACs — including ones genuinely below trust — silently vanish from /spac-arb for the night while the tick logs success and the log lin

**Fix:** Distinguish the three sentinels at the callsite: on `cf === undefined` (transport failure), carry the prior row forward when `priorByCik.has(cik)` — push it into cands with daysStale recomputed via `staleFrom` (row-level STALE-not-EMPTY, matching the feed's own header doctrine) — and increment a separate `fetchFail` counter; a name with no prior row just skips with `fetchFail++`. Keep `cf === null` and present-but-no-us-gaap as genuine absence (`noTrust`). Add a circuit breaker: if `fetchFail` e

## [MEDIUM·failure-vs-absence] preannounce.ts:117 — detectPreannounce: transport failure = "no preannouncement" — silently disables the earnings-play stand-aside, and the missed play is graded into the append-only track record

**Failure:** Nightly refresh-trade-log runs while EDGAR is slow or rate-limiting (any night either bounded fetch exceeds 6s, or fetchWithRetry exhausts retries on 429/5xx). A name that filed an 8-K Item 2.02 ten days before its scheduled print — squarely in the 2-35d window of isPreannouncement8K — screens rich vs its own history (IV bid for the known release). detectPreannounce times out to null (preannounce.ts:95-98/117) and memoizes it (118). refresh-trade-log.ts:165 passes null; the earningsTrade.ts:209 both-sides stand-aside never fires; a short-premium play with legsData is logged awaiting_print (lin

**Fix:** Make detectPreannounce three-valued, matching the secFrames detectorOk pattern: return undefined (or {ok:false}) on any transport failure/timeout — including a null from the 6s race and a null CIK when the ticker-map load itself failed — and null ONLY after a successfully-parsed submissions read found no qualifying 8-K; never memoize failures (or memoize them separately with no persistence). In refresh-trade-log.ts pass 1: on unknown status, skip logging that name tonight — byId.has(id) stays fa

## [MEDIUM·failure-vs-absence] analystActions.ts:32 — getAnalystActions discards the coverage signal built to solve exactly this — four nightly scripts consume a 429 storm as "a quiet week"

**Failure:** Nightly full rebuild at 22:47 UTC runs while Yahoo is rate-limiting the NAS: of the 140 quoteSummary calls in getAnalystActionsDetailed's pool, 120 throw 429 (non-retried by design) and are swallowed at lib/analystActions.ts:77-79; the 20 survivors yield 9 well-formed action rows. getAnalystActions hands those 9 rows to all four scripts with no coverage signal and no rejection. refresh-desk-note.ts publishes desk-note.json with the ANALYST ACTIONS section omitted (empty list → block() returns "" at :275) and counts.analyst=0..2 persisted as fact — a day with three S&P 500 downgrades reads as "

**Fix:** Point the four scripts at getAnalystActionsDetailed and gate on the same threshold the route already uses (attempted > 0 && ok/attempted >= 0.8). Below threshold, honor STALE-not-EMPTY per consumer: refresh-confluence and refresh-warnings should skip the rebuild entirely (log "analyst coverage N/M — keeping prior board" and return before the write), which both preserves the prior board file and — critically — prevents refresh-signal-log from logging a throttle-distorted membership into the unreb

## [MEDIUM·failure-vs-absence] refresh-vol-tags.ts:80 — A per-name news-fetch failure deletes the name's previously persisted catalyst tag from vol-dislocation.json

**Failure:** Night N: name XYZ (IV/RV 1.8x, non-earnings) is tagged {text:"pending buyout", kind:"event"} in data/vol-dislocation.json. Night N+1: refresh-vol-dislocation fails (e.g., putwrite.json refresh died, or writeFeedGuarded blocked — exit 1, continue-on-error proceeds), leaving the stale tagged file. refresh-vol-tags runs; 20 of 24 targets fetch news fine, but both Google News queries for XYZ hit 429s under the 24-way concurrent burst → getNews returns [] (lib/news.ts:113-118), heads:"" (refresh-vol-tags.ts:45), XYZ filtered from withNews (line 50), absent from bySym, hits `delete r.catalyst` (line

**Fix:** Two parts, honoring STALE-not-EMPTY with an expiry. (1) Make failure distinguishable in getNews (the secFrames pattern): track whether any query succeeded and return undefined/throw when ALL queries failed, so callers can tell transport death from genuinely-empty; in refresh-vol-tags, only `delete r.catalyst` for rows actually presented to the model (in withNews) — for fetch-failed rows, keep/restore the prior tag. (2) Because refresh-vol-dislocation rebuilds rows from scratch, gating the delete

## [LOW·cross-feed-clock] refresh-trade-log.ts:68 — closeOn's fallback settles at a price up to 4 days AFTER expiry when no bar exists on-or-before it — halted names get post-expiry resumption prices stamped as spotAtExpiry

**Failure:** A logged short-put name (e.g. a $1bn+ name under a regulatory/news-pending halt) is halted from 8+ days before its option expiry — covering both the print and every session in closeOn's [expiry-8d, expiry+1d) lookback — and resumes trading 2 days after expiry, gapping down 40%. reactionFromChart never found a post-print bar, so the rec is still awaiting_print. On the next nightly run, closeOn's onOrBefore filter is empty, the fallback returns the post-resumption close (-40%), and lines 305-315 stamp it as spotAtExpiry, compute pnlToExpiry via settleLegs at that price, and — because status !== 

**Fix:** In closeOn (scripts/refresh-trade-log.ts:68), never settle at a post-expiry bar: return onOrBefore.length ? onOrBefore[onOrBefore.length-1].close : null — deferral is already the correct downstream behavior (pnlToExpiry stays null, rec_needsExpiry retries nightly), matching the defer-don't-fabricate posture of the VRP fix. To let long-halt names eventually fill with a LEGITIMATE on-or-before mark, widen period1 (e.g. expiry-20d) so the last pre-halt close is findable — that close is the honest a

## [LOW·cross-feed-clock] refresh-trade-log.ts:126 — vixAtLog frozen from macro.json with no stamp check — a stale macro feed stamps a days-old VIX as the log-night regime in the permanent research record

**Failure:** Night N: FRED fetch degrades (the documented lib/fred.ts:72 IPv6/slow-link class) → build-macro.ts:46-53 keeps the prior macro.json (VIX 14, asOf night N-1) and exits 1. Nights N..N+3: a vol spike puts real VIX at 28, but refresh-trade-log.ts:126-129 reads 14 from the stale file each night and line 205 stamps vixAtLog=14 into every play logged (up to CAP=90/night). Line 147 never re-logs an id and the backfill pass deliberately never touches VIX, so when the record is later sliced by regime, the highest-vol cohort in the book is permanently filed under the calm bucket — and the true log-night 

**Fix:** Read the vix indicator's own asOf (observation date) and/or the payload's top-level asOf alongside the value in refresh-trade-log.ts; stamp vixAtLog only when the observation is within a short grace of the log night (e.g. asOf within ~4-5 calendar days to cover weekends/holidays, mirroring the VRP ledger's coneFreshEnough pattern), else leave it null — the code's own contract already defines null as the honest degradation ("a missing macro file costs the annotation, never the log"), so this abst

## [LOW·failure-vs-absence] refresh-spac-arb.ts:92 — SPAC universe enumeration unions only the frames that loaded — rows for CIKs visible solely in failed frames drop without carry

**Failure:** SEC data.sec.gov throttles/flakes for ~10 minutes mid-enumeration on a full-tick night. A SPAC that IPO'd in Q1 2026 and tags only AssetsHeldInTrustNoncurrent appears in exactly two of the twelve frame requests — (CY2026Q2I, Noncurrent) = request #4 and (CY2026Q1I, Noncurrent) = request #7. Both fall inside the flake window (each failed request itself consumes ~93s of it), so both return undefined: failedFrames=2, okFrames≥8 → the line-87 total-failure exit does not fire, detectorOk=false merely forces fresh companyfacts pulls for the CIKs that WERE enumerated. The victim CIK is absent from ci

**Fix:** In refresh-spac-arb.ts, when failedFrames > 0, union the enumeration with the prior file's CIKs before slicing: move the `prior`/`priorByCik` load above line 92, then `if (failedFrames > 0) for (const k of priorByCik.keys()) cikSet.add(k)`. This is correctness-safe by construction: the carried CIKs cannot hit the reuse branch (detectorOk is already false whenever the union triggers), so they take the fresh companyfacts path — a genuinely de-SPAC'd/liquidated name still exits naturally via the ex

## [LOW·cross-feed-clock] refresh-buybacks.ts:218 — Buyback yields priced against the sp500 snapshot with no stamp check while generatedAt advances

**Failure:** Night N: the quote refresh fails (e.g., the live e9c65391 Yahoo egress block) → build-data.ts:560-564 keeps data/sp500/snapshot.json with generatedAt from night N-1 or older. Same night: run-tick.ts:91 runs refresh-buybacks anyway (no dependency gating); the SEC-only step succeeds, passes its only input check (line 219, stocks.length), computes buybackYield = buybackTtm / stale marketCap and totalYield with stale dividendYield (lines 201-208), persists stale price/marketCap per row (line 204), and writes buybacks.json stamped generatedAt = tonight (line 319) through the count-only feedGuard. d

**Fix:** After loadSnapshot (refresh-buybacks.ts:218), compute the snapshot's age from snap.generatedAt vs the run clock. If older than a threshold (~36h): log a loud warning, and stop claiming tonight's clock — carry the price date into the payload (e.g., pricedAsOf: snap.generatedAt alongside generatedAt, or stamp generatedAt = snap.generatedAt) so the freshness registry entry for buybacks.json goes honestly red instead of false green. Do NOT skip or block the write — the board must still publish (STAL

## [LOW·other-integrity] refresh-valuation-history.ts:534 — 'current' multiple is pinned to the first close after the latest quarter-end, contradicting the stated price-fresh nightly recompute

**Failure:** Today Aug 15; a name's latest paneled quarter ended Jun 30 and it sold off 25% in August. Tonight's run re-fetches its full price history through Aug 14, but closeAtOrAfter(quotes, Jun-30) binary-searches to the Jul-1 close — the same close as every prior night — so its published P/S `current`, discountPct, and z in data/valuation-history.json are byte-identical to a week ago while generatedAt stamps tonight. /valuation-history shows it flat-to-rich vs history, refresh-confluence's "Cheap vs 10yr" signal (z<=-1 && discount<=-10) never fires, and refresh-warnings can keep flagging it "Rich vs 1

**Fix:** This needs an explicit intent decision, not a silent patch. Option A (make the code match the three comments): in computeFromPanel, after building points, re-price ONLY the terminal point's numerator at the last available close (mcap_now = quotes[last].c × adjShares of the newest quarter; same TTM denominators, same balance items) — historical points stay quarter-end-priced so the median/p25/p75/z construction is unchanged; set the name's asOf to the price date used and keep the CURRENT_BAND + |

## [LOW·failure-vs-absence] refresh-spac-arb.ts:76 — Partial frame map only disables the detector — universe enumeration still treats it as complete

**Failure:** Night N: SEC throttles data.sec.gov; the CY2026Q2I fetch for AssetsHeldInTrustNoncurrent fails all 3 retries (reset/429/timeout -> secJson returns undefined, line 76: failedFrames++) while the other 11 frames load (okFrames >= 1, so line 87 does not abort). A SPAC that IPO'd in Q2 and has filed exactly one 10-Q tagging AssetsHeldInTrustNoncurrent appears in no loaded frame, so it never enters cikSet (line 82), never enters the per-name loop (line 124), and its prior row cannot be reused (priorByCik is only consulted at line 127, inside the loop it never reaches). The payload is written at line

**Fix:** In scripts/refresh-spac-arb.ts, after building cikSet (line 86) and loading priorByCik (move the prior-file read above the enumeration, or just before line 92): when failedFrames > 0, union cikSet with priorByCik.keys(). This degrades enumeration stale-not-absent: prior names re-enter the universe, and because detectorOk is already false on that branch they ALL take the fresh companyfacts path (line 141) — companyfacts is the source of truth, so no stale floor is reused and correctness is never 

## [LOW·failure-vs-absence] backtest-signals.ts:21 — Backtest treats a partially-loaded series store as the full S&P 500 and writes unguarded

**Failure:** One night the NAS hydration is partial (or the Eufy stack saturates IO): data/sp500 snapshot reads fine but 380 of 503 data/series/symbols/*.json reads fail — ENOENT on the clobbered subtree or EIO/truncated JSON under load. lib/data.ts:85-97 returns null for each; backtest-signals.ts logs "123/503 S&P 500 names have ≥320 stored daily bars" and proceeds. buildPanel's relative floor (max(10, 0.6×123)=74 names/day) builds a calendar from the survivors; every rebalance has ~120 states ≥ 50, so runBacktest returns a full BacktestFile stamped universe:"sp500" whose edges/hit-rates/curves are comput

**Fix:** Mirror the grid's posture, but exit rather than publish: after the load loop, require series.size ≥ max(50, ~0.7×snap.stocks.length) and `process.exit(1)` on shortfall with a loud log — the prior signal-backtest.json then stands and ages honestly toward STALE (never overwrite good with partial). Route line 28 through writeFeedGuarded(OUT, result) for the file-level backstop, and make the registry able to see universe collapse: the countable quantity that actually shrinks is `names` (lib/signalBa
